[build-system]
requires = ["setuptools", "wheel", "cython"]
build-backend = "setuptools.build_meta"


[project]
name = "ruleforge"
version = "0.0.1"
description = "Rule validation engine (cythonizable)"
requires-python = ">=3.8"

